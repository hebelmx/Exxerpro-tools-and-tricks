[project]
name = "exxerpro-tools-and-tricks"
version = "0.0.0"
description = "Curated, anonymized Windows tools, tricks & fixes — built with Zensical"
requires-python = ">=3.12"
dependencies = [
    "zensical>=0.0.45",
]
